import React from "react";
import { NavLink } from "react-router-dom";

const Navbar = () => {
  return (
    <header className="header flex justify-between text-text bg-background">
      <p>Wisconsin Home Helper</p>
    </header>
  );
};

export default Navbar;
